package com.tfr.rulesEngine.example.beer.code;

import com.tfr.rulesEngine.rule._Rule;
import com.tfr.rulesEngine.rule.Rule;

import static com.tfr.rulesEngine.example.beer.code.Styles.*;

/**
 *
 * Created by Erik on 6/27/2017.
 */
public interface StyleRules {
//
//    _Rule<Beer,String> IS_IPA_STYLE = new Rule<>("IPA_RULE", (b) -> b.isStyle(IPA), (b) -> IPA.getName());
//    _Rule<Beer,String> IS_AMBER_STYLE = new Rule<>("AMBER_RULE", (b) -> b.isStyle(AMBER), (b) -> AMBER.getName());
//    _Rule<Beer,String> IS_RED_STYLE = new Rule<>("RED_RULE", (b) -> b.isStyle(RED), (b) -> RED.getName());

}
